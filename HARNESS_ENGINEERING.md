# 하네스 엔지니어링 (Harness Engineering)

> **"AI라는 강력한 엔진에 '핸들'과 '브레이크'를 다는 법"**
>
> 소상공인 AI 경영코치 프로젝트에 적용한 하네스 엔지니어링 실전 가이드

---

## 1. 하네스 엔지니어링이란?

### 탄생 배경

얼마 전까지 AI 업계의 최대 관심사는 **"어떤 모델이 더 똑똑한가?"**였다.

하지만 지금은 상황이 달라졌다:

- **모델들이 너무 비슷해졌다** — GPT, Claude, Gemini 결과물을 나란히 놓으면 구분하기 어려움. 사람 평가 정답률이 동전 던지기 수준.
- **더 키우기도 어렵다** — 성능을 조금 올리려면 학습 비용이 기하급수적으로 증가. 돈을 쏟아부으면 성능이 따라온다는 공식이 더 이상 작동하지 않음.

그래서 업계 전체의 시선이 바뀌었다:

> **"모델 자체를 더 크게 만드는 것 말고, 모델을 더 잘 쓰는 방법은 없을까?"**

이것이 **하네스(Harness) 엔지니어링**이 주목받게 된 배경이다.

### 정의

```
하네스 엔지니어링 = AI 모델을 둘러싼 실행 시스템 전체를 설계하는 기술

말에 마구(Harness)를 씌워 원하는 방향으로 제어하듯,
AI 모델에 '제어 장치'를 씌워 복잡한 일을 안정적으로 수행하게 만드는 외부 실행 구조.
```

핵심 전환:

> **"모델 내부의 성능"에서 → "모델 외부의 시스템 설계"로**

---

## 2. 패러다임의 진화

AI를 잘 쓰는 방법은 세 단계로 진화해왔다:

### 1단계: 프롬프트 엔지니어링 — "질문 잘하기"

같은 AI라도 질문을 어떻게 던지느냐에 따라 결과물 품질이 달라짐.

```
[Before] "마케팅 전략 알려줘"
[After]  "당신은 10년 경력의 F&B 마케터입니다. 서울 강남 카페 매출을 20% 올리는 전략을 제시하세요."
```

**우리 프로젝트 적용**: GPT-4o 프롬프트 설계 (`action_generator.py`, `voice.py`)

### 2단계: 컨텍스트 엔지니어링 — "맥락 설계"

단순 질문을 넘어, AI가 최적 답을 낼 수 있도록 **필요한 정보를 적절한 타이밍과 형태로** 제공.

```
[Before] "친절하게 답해줘"
[After]  회사 소개 + FAQ + 응대 원칙 + 최근 이슈를 체계적으로 정리하여 AI에게 전달
```

**우리 프로젝트 적용**: RAG 파이프라인 (사용자 업종/지역 맥락 자동 제공), 손실 프레이밍 템플릿 (톤앤매너 제어)

### 3단계: 하네스 엔지니어링 — "실행 시스템 설계" ← **현재 단계**

AI가 **스스로 계획을 세우고 → 도구를 사용하고 → 결과를 검증하고 → 다시 시도하는** 구조를 만듦.

```
[Before] 질문 → 답변 (1회성)
[After]  목표 → 분해 → 생성 → 검증 → 재생성 → 완료 (자동 루프)
```

**우리 프로젝트 적용**: SSOT 3종 문서 + 4역할 분업 + 생성-평가 루프 + Claude Code Hooks

---

## 3. 하네스의 핵심 구조

### 3.1 4역할 분업 — "팀으로 일하게 만든다"

잘 설계된 하네스는 하나의 AI가 모든 걸 혼자 처리하지 않는다:

| 역할 | 하는 일 | 건축 현장 비유 |
|------|---------|---------------|
| **Planner** | 큰 목표를 작은 단계로 쪼갬 | 건축가가 도면을 그림 |
| **Generator** | 실제 결과물을 만들어냄 | 시공 팀이 건물을 올림 |
| **Evaluator** | 결과물이 괜찮은지 검증 | 감리사가 품질 검수 |
| **Orchestrator** | 전체 흐름을 조율 | 현장 소장이 공정 지휘 |

```
                    ┌──────────────┐
                    │ Orchestrator │ ← 전체 흐름 총괄
                    │  (현장 소장)   │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ Planner  │ │Generator │ │Evaluator │
        │ (건축가)  │ │ (시공팀)  │ │ (감리사)  │
        │ 목표 분해 │ │ 코드 작성 │ │ 빌드 검증 │
        └──────────┘ └──────────┘ └──────────┘
```

### 3.2 생성-평가 루프 — "반복으로 품질을 높인다"

한 번에 완벽한 답을 내려는 게 아니라, **생성 → 평가 → 재생성** 사이클을 반복:

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│ Generate │────→│ Evaluate │────→│ 결과 OK?  │
│ (코드작성) │     │ (빌드검증) │     │          │
└──────────┘     └──────────┘     └────┬─────┘
      ▲                                 │
      │          NO (실패)               │ YES (성공)
      └─────────────────────────────────┘      │
                                               ▼
                                       ✅ progress.md 업데이트
                                       ✅ git commit

⚠️ 루프는 최대 3회까지만 반복
⚠️ 3회 실패 시 → 에러 분석 + 3가지 대안 제시 → 사용자 선택 대기
```

사람이 글을 쓸 때도 초고를 쓰고, 읽어보고, 고치는 과정을 반복하는 것과 같은 원리. 하네스는 이 과정을 **AI가 자동으로 반복**하게 만든다.

---

## 4. 우리 프로젝트에 적용한 하네스

### 4.1 SSOT 3종 문서 — "진실의 단일 원천"

AI가 참조할 공식 안내서를 프로젝트 루트에 배치:

| 문서 | 역할 | 핵심 내용 |
|------|------|-----------|
| **CLAUDE.md** | 핵심 규칙 + 코딩 스타일 | 코딩 규칙, ADR 6건, 비상 프로토콜, 하네스 전략 |
| **progress.md** | 현재 위치 + 다음 할 일 | Phase 0~9 완료 내역, Todo, 세이브 포인트 4개 |
| **architecture.md** | 구조 지도 + 의존관계 | 시스템 아키텍처, 데이터 흐름, 변경 영향도 매트릭스 |

### 4.2 2-Part 에이전트 분업

'세팅 담당'과 '작업 담당' 에이전트를 분리하여 AI의 인지 부하를 줄임:

```
[Initialization Agent — 세팅 담당]
  1. progress.md 읽기 → 현재 위치 파악
  2. 마지막 성공 커밋 확인
  3. 미완료 Todo 확인 → 작업 계획 수립
  4. CLAUDE.md 규칙 재확인

[Execution Agent — 작업 담당]
  1. Plan → Generate → Evaluate 루프 실행
  2. 단계별 progress.md 업데이트
  3. 최종 검증 + git commit
```

### 4.3 하네스 전략 3단계

| 전략 | 적용 대상 | 리스크 | 예시 |
|------|-----------|--------|------|
| **Strict Harness** | 모든 단계를 인간이 승인 | LOW: 안전성 극대화, 속도 저하 | ADR 변경 금지 항목 (손실프레이밍, k-anonymity) |
| **Adaptive Harness** ★ | 에러 발생 시에만 개입 | MEDIUM: 속도와 안전의 균형 | 일반 기능 개발, 버그 수정 |
| **Vibe Priority** | 하네스 최소화, AI 자율 | HIGH: 빠른 프로토타이핑, 버그 위험 | 아이디어 스케치, 초기 실험 |

> 이 프로젝트는 **Adaptive Harness**를 기본 전략으로 사용.

### 4.4 Claude Code Hooks — 자동화된 가드레일

`.claude/settings.json`에 설정된 자동 실행 훅:

| 시점 | 트리거 | 동작 |
|------|--------|------|
| **PreToolUse** | 파일 Edit/Write 시 | "CLAUDE.md 규칙 준수 확인 필요" 리마인더 |
| **PostToolUse** | 파일 변경 완료 시 | "progress.md 업데이트 잊지 마세요" 리마인더 |
| **Stop** | 작업 종료 시 | "검증 체크리스트 확인" 리마인더 |

### 4.5 비상 프로토콜 — 복붙 프롬프트

#### Context Drift (AI가 규칙을 잊었을 때)
```
"CLAUDE.md를 다시 읽고, progress.md의 현재 위치를 확인한 후,
마지막 성공 커밋부터 다시 시작해."
```

#### Cascading Failure (연쇄 에러)
```
"작업을 중단해. git stash로 현재 변경사항을 임시 저장하고,
마지막 성공 커밋(git log)을 보여줘. 어디서부터 꼬였는지 분석하자."
```

#### 폭주 방지 (3회 이상 같은 에러)
```
"같은 에러가 3번 반복되면 자동으로 멈춰.
에러 원인 분석 + 3가지 해결 방안을 제시하고 내 선택을 기다려."
```

### 4.6 세이브 포인트 — 롤백 전략

| 커밋 | 내용 | 롤백 명령어 |
|------|------|------------|
| `8c64cc5` | 하네스 프레임워크 고도화 | `git revert 8c64cc5` |
| `8a5eb1c` | Known Issues 전체 수정 | `git revert 8a5eb1c` |
| `ac8ecd2` | 문서화 완료 | `git revert ac8ecd2` |
| `79d075b` | Phase 5-6 버그픽스 | `git revert 79d075b` |
| `516ac30` | MVP 전체 구현 | `git revert 516ac30` |

> **꿀팁**: AI에게 일을 시키기 전, "이 작업이 망했을 때 돌아갈 세이브 포인트가 어디인가?"를 먼저 자문.
> `git commit`을 자주 하거나 가이드 문서를 최신화하는 것만으로도 하네스의 절반은 완성.

---

## 5. 실전 결과와 배운 점

### 적용 결과

| 지표 | Before (하네스 없이) | After (하네스 적용) |
|------|---------------------|---------------------|
| Context Drift 발생 | 긴 작업 시 빈번 | SSOT 참조로 방지 |
| 빌드 깨짐 | 코드 변경 후 미검증 | 생성-평가 루프로 자동 검증 |
| 롤백 시간 | "어디서 꼬였지?" 수십 분 | 세이브 포인트로 즉시 복구 |
| 팀원 간 일관성 | 각자 다른 스타일 | CLAUDE.md로 통일 |

### 개발의 본질 변화

```
Before: 코드를 '쓰는 일'
After:  AI가 달릴 '궤도(Harness)'를 '설계하는 일'
```

### 시행착오: 하네스의 과적합

```
⚠️ 규칙을 너무 촘촘하게 만들면:
  → AI의 창의적 해결책(바이브)이 막힘
  → 속도 저하
  → "최소한의 가드레일"과 "자율성" 사이의 균형이 핵심

우리의 균형점:
  - 변경 금지 항목 (ADR 001~003): Strict
  - 일반 기능 개발: Adaptive
  - 프로토타이핑: Vibe
```

---

## 6. 클로드 코드 유출 사건이 증명한 것

### 2026년 3월 31일

Anthropic의 클로드 코드 내부 소스코드 51만 줄이 npm 패키징 실수로 외부에 공개됨.

### 발견된 하네스 설계

- 컨텍스트 넘침 대비 **5가지 압축 전략**
- 비용 최소화 **서브에이전트 구조**
- 사용자 불만을 LLM이 아닌 **단순 패턴 매칭**으로 처리
- 항상 켜진 백그라운드 **KAIROS 기능**

### 시사점

```
연간 2조원+ 매출의 클로드 코드 핵심 경쟁력이
모델이 아닌 "하네스"에 있었다.

= 모델은 누구나 쓸 수 있다.
  차이를 만드는 건 그 모델을 어떻게 연결하는지 여부다.
```

---

## 7. 분석 프레임워크

### 제1원칙 사고 (First Principles)

코딩의 핵심은 '언어 숙련도'가 아니라 **'논리적 인과관계의 통제'**. 하네스는 이 통제력을 인간에게 다시 부여한다.

### 모듈러 설계 (Modular Design)

시스템을 쪼개고 각 모듈 사이에 **검증 계층(Harness)**을 두어, 전체 시스템의 붕괴(Cascading Failure)를 방지한다.

### Assumptions, Reasoning, Implications

| | 내용 |
|---|------|
| **Assumptions** | AI 성능이 좋아질수록 인간의 '검증 능력'이 병목이 될 것 |
| **Reasoning** | 확률적 LLM은 구조적으로 '일관성'을 담보할 수 없으므로, 외부의 '결정론적 시스템(Harness)'이 반드시 필요 |
| **Implications** | 미래의 코딩은 '자연어'로 의도를 말하고, '하네스'로 그 의도가 실현되는지 감시하는 '오케스트레이션' 형태가 될 것 |

---

## 8. 이 프로젝트의 파일 구조

```
ssgi/
├── CLAUDE.md               ← 🎯 핵심 규칙 (SSOT #1)
├── progress.md             ← 📍 현재 위치 (SSOT #2)
├── architecture.md         ← 🗺️ 구조 지도 (SSOT #3)
├── HARNESS_ENGINEERING.md  ← 📖 이 문서 (배경 + 적용 가이드)
├── .claude/settings.json   ← ⚙️ Claude Code Hooks (자동화)
├── README.md               ← 프로젝트 소개
├── VERIFICATION_REPORT.md  ← 검증 결과
├── IMPLEMENTATION_SPEC.md  ← 구현 명세서 (5,870줄)
├── backend/                ← FastAPI (44 .py 파일)
└── frontend/               ← Next.js (13 .ts/.tsx 파일)
```

---

## 9. 앞으로의 계획

1. **MVP 실연동 테스트** — API 키 확보 후 E2E 유저 여정 검증
2. **사용자용 하네스 UX 연구** — 시니어/디지털 소외 계층이 AI 사용 시 겪는 심리적 불안 해소
3. **시각적 모니터링 대시보드** — 하네스 상태(현재 Phase, 에러 카운트, 세이브 포인트)를 실시간 확인
4. **자동 롤백 워크플로우** — 빌드 실패 시 마지막 성공 커밋으로 자동 복구

---

## 참고 자료

- Andrej Karpathy의 "Vibe Coding" 트윗 및 관련 논의
- Claude Code 소스코드 유출 분석 (2026.03.31)
- 테크뷰 AI 하네스 엔지니어링 개념 정리
- 지피터스 AI 자동화 및 에이전트 설계 사례

---

> **마지막 업데이트**: 2026-04-06
>
> **작성**: 정수현 (PM/데이터) + Claude AI Agent
